// Androidのシステムナビゲーションバー（3ボタン/ジェスチャー）の高さを保持する。
// ビューアはイマーシブモード(SystemUiMode.immersiveSticky)でバーを隠すため、
// バー表示中は0になるはずのMediaQuery.padding.bottomがビューア表示中はずっと
// 0を返してしまい、「バーがある時と同じ高さ」を計算する基準に使えない。
// バーが実際に表示されている（本棚などの非ビューア画面がビルドされた）瞬間の
// 値を都度キャッシュしておき、ビューア側はこの値を使ってポップアップを
// 他のページと同じ高さまで持ち上げる
class SystemNavBarInset {
  static double bottom = 0;
}
